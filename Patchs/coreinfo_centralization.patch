diff --git a/payloads/coreinfo/coreinfo.c b/payloads/coreinfo/coreinfo.c
index b01dfebd88..233cfae800 100644
--- a/payloads/coreinfo/coreinfo.c
+++ b/payloads/coreinfo/coreinfo.c
@@ -294,8 +294,11 @@ int main(int argc, char **argv)
 	init_pair(2, COLOR_WHITE, COLOR_BLACK);
 	init_pair(3, COLOR_BLACK, COLOR_WHITE);
 
-	modwin = newwin(SCREEN_Y - 3, SCREEN_X, 1, 0);
-	menuwin = newwin(2, SCREEN_X, SCREEN_Y - 2, 0);
+	int start_x = (COLS > SCREEN_X) ? (COLS - SCREEN_X) / 2 : 0;
+	int start_y = (LINES > SCREEN_Y) ? (LINES - SCREEN_Y) / 2 : 0;
+
+	modwin = newwin(SCREEN_Y - 3, SCREEN_X, start_y + 1, start_x);
+	menuwin = newwin(2, SCREEN_X, start_y + SCREEN_Y - 2, start_x);
 
 	wattrset(stdscr, COLOR_PAIR(1) | A_BOLD);
 	wattrset(modwin, COLOR_PAIR(2));
